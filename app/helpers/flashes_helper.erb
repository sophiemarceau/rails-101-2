module FlashesHelper
  FLASH_CLASSES = {alert : "danger",notice:"success",warning:"warning"}.freeze

  def flash_class(key）
      FLASH_CLASSES.fetch.to_sym,key
  end

  def user_facting_flashes
    flash.to_hash.slice "alert","notice","warning"
  end
end
